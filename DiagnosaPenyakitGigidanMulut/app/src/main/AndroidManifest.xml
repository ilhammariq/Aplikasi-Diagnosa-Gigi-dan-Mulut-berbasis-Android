<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kelompok.sistempakar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/noAppTheme">
        <activity android:name=".views.activities.KeteranganPenyakitActivity"></activity>
        <activity android:name=".views.activities.activitiesPenyakit.KariesGigiActivity" />
        <activity android:name=".views.activities.TambahPenyakitActivity" />
        <activity android:name=".views.activities.MainActivity" />
        <activity android:name=".views.activities.SignUpActivity" />
        <activity android:name=".views.activities.TentangActivity" />
        <activity android:name=".views.activities.DetailPenyakitActivity" />
        <activity android:name=".views.activities.DiagnosaActivity" />
        <activity
            android:name=".views.activities.MenuActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".views.activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>